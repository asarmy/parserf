- okay for a given site lat/lon, i will get all RUPTURES within say 150 km
- I will go through the ruptures to get all of the PARENT FAULTS involved
- and I will initiate magnitude bins to hold magnitude probability mass distributions for each PARENT FAULT
- then i will go through each rupture and partition the rate into each PARENT FAULT into the magnitude bin (we are just holding rates--or fractions thereof-- in the magnitude bins)
- Now I have a suite of PARENT FAULTS and magnitude-rate distributions
- For each PARENT FAULT I need to normalize the magnitude-rate bins so that it is a probability mass that sums to unity (we should drop empty bins)
- Separately, for each PARENT FAULT, i need to extract the cumulative MFDs for each subsection; specifically I want the activity rate (rate at Min) mean and stdv
- For each PARENT FAULT, I also want dip, style, seismo top, seismo bottom, coordinates (linestring geom)
- So the final product is a geodataframe with the following columns:
	- name (of the parent fault)
	- dip
	- style
	- seismo top
	- seismo bottom
	- coordinates (linestring geom)
	- mean_rate
	- stdv_rate
	- dictionary of magnitude probability mass bins


======
How will I use this package?
- to get info on a specific subsection, including coordinates/shapely object, upper/lower depth, and participating rupture info (magnitudes, rates, cumul mfd, rupture lengths, parent area breakdown). I like having FaultSubsection and the Data and Ruptures layers. I will also want to know what the closest subsection is for a lat/long and get info on it. It is find to separate that query from these classes.
- to get info on a specific parent fault, including info on the subsections in it like dip, upper/lower depth, merged surface trace (orientation-adjusted), style, and participating rupture info (magnitudes, rates, cumul mfds of its subsections). I like having the ParentFault and Data and Rupture layers.
- another important use for this package is that for a given lat/lon, i will want to get all RUPTURES within some distance, say 150 km. I will need to go through the ruptures to get all of the PARENT FAULTS involved, and I will use a bunch of information on these ruptures in a separate python package. I will use the following information for each parent fault in that package:
	- name (of the parent fault)
	- dip (probably area-weighted average of all the subsections)
	- style
	- seismo top (probably area-weighted average of all the subsections)
	- seismo bottom (probably area-weighted average of all the subsections)
	- coordinates (linestring geom, oriented)
	- magnitude, rate, cumul_mfds, and parent area percents for each rupture the parent fault participates in
For this last use case, we can leave a lot of the heavy lifting to the separate python package, but the point is to make this package flexible enough to handle it. Can we make this happen, while maintaining these good design principles I am focused on?


======
what's left on parserf? create docs with sphinx. not sure if i can just do this locally because the repo is private. then bump to v1 even if it remains private. maybe an mcp for working with hz flt?

y recommendation

  Start with option 1 + 2 combined. In ROOT/package2/CLAUDE.md:

  @/Users/alex/dev/parserf/CLAUDE.md

  When you need detailed parserf API docs beyond the above summary,
  read from /Users/alex/dev/parserf/docs/html/

  This gives Claude Code the architectural mental model automatically (via the import) and a path to detailed API docs when
  needed. Zero infrastructure, and the CLAUDE.md you already have is quite thorough about the module structure, data flow,
  and key patterns.

  An MCP server would only become worthwhile if you have multiple local packages with large API surfaces and find that
  Claude Code frequently misuses the APIs despite having the docs path.
